INICIAR O SERVIDOR NO SEU PC:
Abre a pasta "servidor-cardapio" (onde ta o cardapio.json e as fotos).
Abre o terminal dentro da pasta.
cola isso pra iniciar o servidor:
  npx http-server -p 8080

Agora é só rodar o app.
